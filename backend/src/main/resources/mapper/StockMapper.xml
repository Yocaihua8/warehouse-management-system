<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yocaihua.wms.mapper.StockMapper">

    <select id="selectByProductId" resultType="com.yocaihua.wms.vo.StockVO">
        SELECT
        p.id AS product_id,
        p.product_code,
        p.product_name,
        p.specification,
        p.unit,
        p.category,
        s.quantity,
        s.warning_quantity,
        CASE
        WHEN s.quantity &lt;= s.warning_quantity THEN 1
        ELSE 0
        END AS low_stock
        FROM product p
        LEFT JOIN stock s ON p.id = s.product_id
        WHERE p.id = #{productId}
    </select>

    <select id="countStock" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM product p
        LEFT JOIN stock s ON p.id = s.product_id
        <where>
            <if test="productCode != null and productCode != ''">
                p.product_code LIKE CONCAT('%', #{productCode}, '%')
            </if>
            <if test="productName != null and productName != ''">
                AND p.product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
        </where>
    </select>

    <select id="selectStockPage" resultType="com.yocaihua.wms.vo.StockVO">
        SELECT
        p.id AS product_id,
        p.product_code,
        p.product_name,
        p.specification,
        p.unit,
        p.category,
        s.quantity,
        s.warning_quantity,
        CASE
        WHEN s.quantity &lt;= s.warning_quantity THEN 1
        ELSE 0
        END AS low_stock
        FROM product p
        LEFT JOIN stock s ON p.id = s.product_id
        <where>
            <if test="productCode != null and productCode != ''">
                p.product_code LIKE CONCAT('%', #{productCode}, '%')
            </if>
            <if test="productName != null and productName != ''">
                AND p.product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
        </where>
        ORDER BY p.id ASC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="selectStockList" resultType="com.yocaihua.wms.vo.StockVO">
        SELECT
        p.id AS product_id,
        p.product_code,
        p.product_name,
        p.specification,
        p.unit,
        p.category,
        s.quantity,
        s.warning_quantity,
        CASE
        WHEN s.quantity &lt;= s.warning_quantity THEN 1
        ELSE 0
        END AS low_stock
        FROM product p
        LEFT JOIN stock s ON p.id = s.product_id
        <where>
            <if test="productCode != null and productCode != ''">
                p.product_code LIKE CONCAT('%', #{productCode}, '%')
            </if>
            <if test="productName != null and productName != ''">
                AND p.product_name LIKE CONCAT('%', #{productName}, '%')
            </if>
        </where>
        ORDER BY p.id ASC
    </select>

    <update id="updateByProductId" parameterType="com.yocaihua.wms.dto.StockUpdateDTO">
        UPDATE stock
        SET
        quantity = #{quantity},
        warning_quantity = #{warningQuantity}
        WHERE product_id = #{productId}
    </update>

    <update id="decreaseStock">
        UPDATE stock
        SET quantity = quantity - #{quantity}
        WHERE product_id = #{productId}
        AND quantity >= #{quantity}
    </update>

    <update id="increaseStock">
        UPDATE stock
        SET quantity = quantity + #{quantity}
        WHERE product_id = #{productId}
    </update>

    <update id="increaseQuantity">
        UPDATE stock
        SET quantity = quantity + #{quantity}
        WHERE product_id = #{productId}
    </update>

    <insert id="insertInitStock">
        INSERT INTO stock (
        product_id,
        quantity,
        warning_quantity
        ) VALUES (
        #{productId},
        #{quantity},
        #{warningQuantity}
        )
    </insert>

    <delete id="deleteByProductId">
        DELETE FROM stock
        WHERE product_id = #{productId}
    </delete>


</mapper>
